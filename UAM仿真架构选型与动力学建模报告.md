# UAM 仿真架构选型与动力学建模技术报告

> 来源：飞书 wiki（用户 2026-07-19 粘贴整理，为《8 旋翼异构全驱动 UAM 仿真控制方案》的配套对比/分析报告）
> 说明：全文按原文结构整理；文中多处「暂时无法在飞书文档外展示此内容」为飞书图片/表格块未随文本导出，已在对应位置标注缺失内容；文末附缺失项清单与跨文档一致性提示。

---

## 1. 核心仿真架构横向对比

> ⚠️ **缺失表格**：本节原文含一张「Gazebo / Isaac Sim / AirSim(Colosseum) 三大仿真栈横向对比表」（渲染风格、多体动力学、接触力学、RL 支持、生态等维度），未随文本导出。下方为文字论述部分。

### 💡 选型建议

- 如果你要开发 ROS 2 导航集群、传统控制算法、物流自动化业务 ➡️ 选择 **Gazebo + PX4 + QGC**。
- 如果你要训练具身智能、大模型驱动的无人机视觉感知、深度强化学习 ➡️ 选择 **NVIDIA Isaac Sim**。
- 如果你有现成的 UE 场景资源，且极其依赖 AirSim 现成的 Python API 进行上层开发 ➡️ 选择 **AirSim 衍生版 (Colosseum) + PX4 + QGC**。

如果你的核心场景是「无人机 + 挂载机械臂」，那么 **AirSim 绝对不是最好的选择，甚至可以说完全不适用**。

因为机械臂在运动时，其质心（CoM）和转动惯量会实时剧烈变化；而在抓取目标时，接触力矩会直接反馈到无人机机身上。AirSim 的底层物理模型是高度**为单体无人机定制的刚体航空动力学**，原生极难支持多刚体铰接动力学（Multi-body Articulated Dynamics）和复杂的接触力学。

针对「无人机 + 机械臂」的场景，目前行业内公认最强的两个仿真技术栈如下：

### 方案一：学术界与传统控制首选 —— Gazebo + ROS 2 + MoveIt 2 + Octorotor/Quadrotor Model

本节论述的渲染风格和底层物理需求，最契合的正是 Gazebo。

- **为什么最合适：**
  - **强大的多体动力学**：Gazebo 原生支持通过 URDF / SDF 文件定义复杂的树状关节拓扑结构。你可以完美定义无人机基座与机械臂每个关节（Link & Joint）的质量、惯性张量和物理限制。
  - **全闭环动力学耦合**：机械臂挥动时产生的反作用力和力矩，会自动通过物理引擎（如 DART 或 ODE）实时作用于无人机机身。
  - **生态完全打通**：你可以用 MoveIt 2 来规划机械臂的轨迹，用 ROS 2 控制器发送关节外力/速度，同时用坐标转换（TF）和控制器实时补偿无人机因机械臂运动带来的重心偏移。
- **飞控配合**：在这种场景下，传统的 PX4 固件（仅把无人机当成标准四轴）往往需要极其复杂的调参或者修改底层混控（Mixer）。学术界和前沿工业界更多会自己写基于 ROS 2 的几何控制（Geometric Control）或模型预测控制（MPC）算法来维持悬停稳定。

### 方案二：强化学习与高精度接触首选 —— NVIDIA Isaac Sim / Isaac Lab

如果你打算通过深度强化学习（DRL）来让无人机协同机械臂完成「飞过去 ➡️ 抓取 ➡️ 带走」的端到端动作，Isaac Sim 是目前的绝对天花板。

- **为什么最合适：**
  - **PhysX 5 的多体关节支持**：Isaac Sim 的底层物理引擎对铰接体（Articulated Bodies）的摩擦力、接触力、碰撞检测进行了极致的 GPU 加速优化。机械臂抓取瞬间的物理碰撞和滑移模拟非常精准。
  - **端到端训练**：无人机的 6-DoF 位置控制与机械臂的 N-DoF 关节控制可以并入同一个神经网络的 Action Space 中，通过并行仿真在极短时间内学会如何「在飞行的同时保持臂的平衡」。
- **劣势**：模型搭建和控制器编写门槛比 ROS + Gazebo 更高。

### 🛠️ 「无人机 + 机械臂」仿真的核心避坑指南

1. **重心（CoM）动态补偿**：机械臂展开时，无人机的质心会发生偏移。如果你直接用原生的 PX4 位置控制，无人机会发生严重的倾斜甚至炸机。必须在仿真中实现一个前馈补偿算法，读取机械臂当前关节角，计算出当前的整体质心，并实时调整无人机各电机的基础转速。
2. **接触力反馈（Contact Force）**：抓取目标（抓取、拧螺丝、触碰悬挂物）产生的瞬间冲击力是 UAM 仿真最难的部分。Gazebo 的 DART 物理引擎或 Isaac Sim 的 PhysX 5 在处理这种「硬接触」时的稳定性明显优于 AirSim 的物理近似。

### 💡 关键选型建议

- 如果你的核心任务是验证控制理论、编写多体动力学补偿算法、做传统的轨迹规划： ➡️ 建议直接采用 **Gazebo (Classic 或最新的 Harmonic) + ROS 2 + MoveIt 2**。这是这条路上前人踩坑最多、开源代码最全的路线。
- 如果你的核心任务是通过视觉和强化学习，训练机械臂在空中抓取动态物体的智能体（Agent）： ➡️ 建议直接上 **NVIDIA Isaac Sim / Isaac Lab**。

---

## 2. 空中操纵系统动力学建模与闭环动力学耦合分析

空中操纵系统（Unmanned Aerial Manipulators, UAM）代表了现代自主机器人领域中一类高耦合、非线性的浮动基座多体动力学系统。在传统的地面操作任务中，机械臂的基座通常是固定在刚性地表或重型移动底盘上的，而空中操纵系统则将多自由度（DoF）关节机械臂挂载于高机动性的多旋翼无人机（UAV）基座之上。这种构型虽然极大地扩展了工作空间，但也引入了极其复杂的物理交互和动态干扰。

为了在仿真环境中实现高逼真度的物理再现，Gazebo 仿真引擎提供了关键的多体物理求解支持。在系统建模阶段，设计人员通过统一机器人描述格式（URDF）或仿真描述格式（SDF）来定义多旋翼基座以及机械臂各个关节（Link & Joint）的质量、惯性张量、几何碰撞边界以及物理关节限位。由于 URDF 本身在描述复杂的多体世界交互（如非机械连接的物理碰撞、接触力等）上存在局限性，Gazebo 通常会在底层自动将 URDF 解析并转换为 SDF 拓扑结构，以支持更加复杂的动态约束求解。

空中操纵系统在作业时，机械臂的运动会产生强烈的反作用力和力矩，这些力矩通过物理关节实时作用于无人机机身。这种物理耦合可以描述为**全闭环动力学耦合**，其关键挑战在于以下两个物理现象：

- **质心动态漂移（CoM Migration）**：机械臂关节角度的变化会直接改变整个 UAM 系统的总质心位置。在悬停状态下，质心偏移会导致非对称的重力矩，若控制系统未进行前馈补偿，多旋翼基座将会产生非自主性的倾斜或平移。
- **反作用 wrench 传递**：机械臂各连杆的加速和减速运动，会向无人机底座施加动态反作用力矩。这些高频力矩很容易导致传统的反馈控制系统失稳，从而在空气动力学下引发严重的极限环振荡。

为了在仿真中精确解算上述物理过程，物理引擎的选择至关重要。常用的物理引擎包括开源动力学引擎（ODE）和动态动画与机器人工具包（DART）。

> ⚠️ **缺失表格**：原文含「ODE vs DART 在处理空中操纵系统时的数值表现与适用性对比表」（求解器类型、接触稳定性、铰接体支持、计算效率、适用场景等维度），未随文本导出。

空中操纵系统在物理构型上可分为**欠驱动基座（Underactuated Base）**与**全驱动基座（Fully Actuated Base）**。这两者在应对上述动力学耦合时，其控制难度和物理特性存在本质差异：

> ⚠️ **缺失表格**：原文含「欠驱动基座 vs 全驱动基座对比表」（自由度解耦能力、姿态保持、抗反作用 wrench、控制带宽、典型实现等维度），未随文本导出。

---

## 3. 软件架构设计与飞控和操纵栈的集成方案

构建高精度空中操作仿真系统，需要将高层轨迹规划算法、中层关节控制器以及低层飞控动力学解算器进行深度融合。基于 ROS 2 Humble 机器人中间件，系统设计采用模块化、分布式的总线通信架构，确保数据的高吞吐量和低延迟传输。

在操纵栈方面，系统利用 `ros2_control` 框架作为硬件抽象层。通过在 Gazebo 仿真环境中加载 `gz_ros2_control`（或在旧版 Ignition Fortress 下加载 `ign_ros2_control`）插件，可在物理引擎中直接实例化一组虚拟的硬件接口（Hardware Interfaces）。

`joint_trajectory_controller` 负责订阅关节轨迹规划，并将计算得出的位置或扭矩指令以高频（如 1000 Hz）发送至仿真关节执行器。`MoveIt 2` 控制器栈则位于最上层，通过 OMPL 规划算法计算无碰撞、符合运动学限位的关节轨迹，再将轨迹序列分发给 `ros2_control` 的控制器执行。

在飞控集成方面，传统的开源飞控如 PX4 或 ArduPilot 的原生固件主要针对对称的非铰接刚体设计，其内置的级联 PID 算法无法预测和有效抑制空中机械臂运动产生的高频动态扰动。虽然可以通过定制 PX4 的底层混控器（Mixer）或开发特定的 uORB 消息处理模块来缓解该问题，但在前沿学术界与先进工业界中，更为普遍的做法是绕过传统的飞控算法，直接基于 ROS 2 实现集中式的多旋翼几何控制（Geometric Control）或模型预测控制（Model Predictive Control, MPC）。

集中式的**端效应器中心（End-Effector-Centric）整体动力学 MPC（Whole-Body MPC）**将空中机身与多自由度关节机械臂作为一个整体进行状态预测。其控制输入直接映射为多旋翼各个电机的期望转速以及机械臂各个关节的轴向扭矩。

由于浮动基座受制于空气动力学效应、旋翼下洗流（Downwash）及接触力学的不确定性，控制器中通常还会引入 $L_1$ 自适应控制模块。$L_1$ 自适应滤波器能够对系统建模误差、未知有效载荷以及阵风干扰进行瞬时估计，并在反馈通道中快速应用低通滤波后的控制量进行补偿，确保整体系统在复杂的接触物理交互（如抓取、推拉、敲击）中维持控制回路的鲁棒性。

ROS 2 与 Gazebo 物理仿真环境之间的数据和时钟同步由 `ros_gz_bridge` 提供支持。该桥接层可将 Gazebo 内部发布的原始物理仿真主题双向转换为标准的 ROS 2 消息流，其消息传输时钟以仿真的物理时钟（`/clock`）为基准，从而消除了仿真系统慢于物理现实时可能产生的时序错乱。

> ⚠️ **缺失表格**：原文含「主流开源核心组件、仿真框架、顶尖学术研究代码仓库地址与软件环境要求汇总表」（组件名、仓库 URL、ROS 版本、物理引擎、备注等维度），未随文本导出。

---

## 4. 基于 RoboStack 与 Conda 的标准化环境配置与部署 SOP

本节提供在 Ubuntu 22.04 主机上，利用 RoboStack 及 Conda 构建完全隔离、无需 Root 权限的 ROS 2 Humble + Gazebo Ignition Fortress 仿真环境的标准作业流程（SOP）：

### 1. 系统底层依赖包更新与 OpenGL 加速准备

在终端中执行系统包更新，并安装 OpenGL 所需的底层图形库，以保证仿真环境中 Gazebo 3D 渲染和 RViz 视觉处理能够正常调度 GPU 硬件加速：

```bash
sudo apt-get update && sudo apt-get upgrade -y
sudo apt-get install -y libgl1-mesa-dri libglx-mesa0 libegl-mesa0 build-essential git curl
```

### 2. Conda (Miniforge) 运行时安装

从官方服务器拉取最新的 Miniforge 安装脚本并初始化 shell：

```bash
curl -L -O "https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-$(uname)-$(uname -m).sh"
bash Miniforge3-$(uname)-$(uname -m).sh -b -p $HOME/miniforge3
# 激活当前终端的 conda 运行变量
eval "$($HOME/miniforge3/bin/conda shell.bash hook)"
mamba init bash
```

> 提示：安装完成后，务必关闭当前终端并重新打开，或者直接重新登录 SSH 会话以使得环境变量在 shell 中完全生效。

### 3. 创建专属虚拟仿真环境并配置 RoboStack 软件源渠道

创建一个名为 `uam_sim_env` 的 Conda 虚拟环境（强制指定 Python 3.11），并对该环境的包检索优先级进行配置，防止出现系统级 apt 软件污染：

```bash
mamba create -n uam_sim_env python=3.11 -y
mamba activate uam_sim_env

# 严格配置当前虚拟环境的 Channel 检索顺序
conda config --env --add channels conda-forge
conda config --env --add channels robostack-humble
# 移除默认的 default 渠道，确保所有依赖项都来自 conda-forge 保证 ABI 兼容一致性
conda config --env --remove channels defaults
```

### 4. 一键式安装 ROS 2 核心及仿真插件

利用高性能 C++ 求解引擎 mamba 安装 ROS 2 Humble 桌面版、Gazebo 仿真接口和低层控制器依赖：

```bash
mamba install ros-humble-desktop \
              ros-humble-ros-gz-sim \
              ros-humble-ros-gz-bridge \
              ros-humble-ign-ros2-control \
              ros-humble-joint-state-publisher-gui \
              ros-dev-tools \
              compilers \
              cmake \
              pkg-config \
              -y
```

安装 OpenGL 共享链接库以防止本地动态库加载失败：

```bash
mamba install -c conda-forge libgl-devel -y
```

### 5. 创建 ROS 2 仿真工作空间并编译本地操纵节点

在激活虚拟环境的基础上，初始化标准的 colcon 编译架构：

```bash
mkdir -p ~/uam_colcon_ws/src
cd ~/uam_colcon_ws/src

# 1. 抓取空中机械臂系统的 URDF 与 Xacro 仿真描述
git clone https://github.com/medhijk/Aerial-Manipulator.git

# 2. 抓取运动规划框架 AM-Planner 核心包
git clone https://github.com/SYSU-HILAB/am-planner.git

cd ~/uam_colcon_ws
# 初始化并更新 rosdep 数据源，用以检查并下载工作空间中未满足的代码级依赖项
rosdep init
rosdep update
rosdep install --from-paths src --ignore-src -r -y --rosdistro humble

# 采用 symlink-install 的编译机制以利于本地快速迭代调试
colcon build --symlink-install --cmake-args "-DCMAKE_BUILD_TYPE=Release"
```

> 为了确保仿真系统的时钟、环境搜索路径以及图形显示窗口能够正确渲染，请在系统激活脚本中做好相关系统配置。

> ⚠️ **缺失表格**：原文含「常见环境配置项表」（配置项、作用、设置位置/命令等维度），未随文本导出。

---

## 5. 模型与测试场景构建

在空中操纵系统仿真中，高保真度的模型和场景是研究控制算法、感知管线和规划策略的基石。在场景端，研究人员可以利用 Gazebo Fuel 在线资源库，其类似于仿真世界的「应用商店」，集成了数以千计由官方或社区分享的预制三维网格场景模型（包括厂房、断裂管道、风力发电机模型等静态环境资产）。用户可以直接在 SDF 描述中通过引入具体的统一资源定位符（URL）将这些模型热加载到仿真世界中，避免了繁重的本地 Mesh 建模。

```
                             [ Gazebo Fuel / 静态网格资产 ]
                                           |
                                           v  (SDF 引用热加载)
+----------------------------------------------------------------------------------------+
|                                    Gazebo 仿真世界 (World)                              |
|                                                                                        |
|    +-----------------------------+               +--------------------------------+    |
|    |      空中操纵系统 (UAM)     | <---(碰撞力矩) |   操作目标物 (如 阀门、电珠)   |    |
|    |                             |               |                                |    |
|    |  +-----------------------+  |               |  - 铰接扭矩限制                 |    |
|    |  |     多旋翼无人机      |  |               |  - 刚体惯性常数                 |    |
|    |  +-----------+-----------+  |               +--------------------------------+    |
|    |              | (Mount)      |                                                     |
|    |              v              |                                                     |
|    |  +-----------------------+  |               +--------------------------------+    |
|    |  |   多自由度关节机械臂   |  |               |      视觉/传感器感知系统       |    |
|    |  +-----------+-----------+  |               |                                |    |
|    |              |              |               |  - 射线投射 LiDAR 探测范围     |    |
|    |              v              |               |  - RGB-D 深度图噪点分布         |    |
|    |  +-----------------------+  |               +--------------------------------+    |
|    |  |       端效应器        |  |                                                     |
|    |  +-----------------------+  |                                                     |
|    +-----------------------------+                                                     |
+----------------------------------------------------------------------------------------+
```

在机械臂模型下载与构建时，本报告以代表性的 **AM-Planner (SYSU-HILAB)** 项目和 **Flying Hand (LeCAR-Lab)** 项目为例：

- 对于避障规划框架 **AM-Planner**，其底层集成了基于扩散 Transformer 的模仿学习算法模型（Polynomial_DiT）。模型需要读取预先训练的权重以执行避障轨迹推理。
- **操作要点**：用户必须导航至本地 checkpoints 目录下下载其预训练好的参数权值文件：

```bash
cd ~/uam_colcon_ws/src/am-planner/dit/Polynomial_DiT/checkpoints
python3 download.py
```

- 对于 **Flying Hand** 整机操作平台，其将全驱动的 Hexarotor 机身和 4 自由度的拟人化关节机械臂通过标准 D-H 参数模型耦合在同一个 Xacro 统一配置文件中。若要在仿真环境加载测试目标（如阀门、电珠、插座等操作对象），应为目标定义铰接扭矩限制和刚体惯性常数，使机械臂通过指尖的摩擦力传感器和压力解算器能施加准确的力矩。

场景和控制算法就绪后，可通过如下指令运行系统级测试：

```bash
# 激活编译空间
source ~/uam_colcon_ws/install/setup.bash

# 1. 运行 UAM 仿真核心世界(此时 Gazebo 与 ROS-GZ Bridge 启动，物理时钟开始广播)
ros2 launch uam_gazebo uam_gazebo.launch.py

# 2. 从另一终端运行整机 MPC 控制及路径规划节点
ros2 launch am_planner run_in_sim_other.launch
```

---

## 6. 算力配置与硬件资源选型指南

仿真空中操纵系统不仅需要物理求解器解算复杂的多铰接链约束，还可能涉及高精度的 RTX 路径追踪渲染、高频控制环、以及视觉传感器物理拟态计算（如生成 RGB-D 图像流、基于物理机制的 Lidar 射线投影等）。为此，必须针对任务的不同粒度进行合理的硬件资源配置。

> ⚠️ **缺失表格**：原文含「三种计算负载要求下的标准化推荐配置参数表」（轻量/中负载/高负载 三档：CPU 主频、核心数、GPU、显存、内存等维度），未随文本导出。

### 算力资源对仿真物理行为的潜在约束分析

- **物理引擎求解步长限制与实时因子（RTF）**：若在 Gazebo 中设置了极细的解算步长（如 $\Delta t = 0.5 \text{ ms}$ 以提升碰撞接触精度），在物理连杆数多、几何边界复杂的空中操纵仿真中，单核 CPU 处理主物理循环的负载将急剧飙升。如果主频不足或未充分开辟多线程解算，实时因子（Real-Time Factor, RTF）将会跌至 $1.0$ 以下。在 RTF 不为 $1.0$ 的非一致性时钟环境下，低层控制器的微分和积分计算如果未绑定仿真的 `/clock`（而是读取了系统的实际挂钟时间），会导致控制器的增益失效，甚至诱发仿真物理中「无人机直接发散坠毁」的数值灾难。
- **传感器数据渲染与显存容量极限**：如果在多旋翼基座和机械臂末端挂载了多路高帧率深度相机（如 Intel RealSense RGB-D 传感器）进行图像视觉伺服，仿真世界需要将所有静态、动态 Mesh 频繁推送至显卡着色器。若显卡显存小于 16 GB，在渲染具有复杂纹理和密集多边形操作物的工厂场景时，会触发显存溢出与交换错误，导致仿真帧率瞬间坍塌（低于 1 Hz），这极大地损害了图像闭环感知控制器的开发效能。
- **硬件 RT 核心限制（RT Cores）**：在探索现代具身智能空地操作（如通过 NVIDIA Isaac Sim 等现代仿真平台训练 UAM 自主强化学习策略）时，仿真底层必须借助 RTX 渲染技术来逼近物理光学一致性，从而跨越 Sim-to-Real 的鸿沟。NVIDIA 官方指出，诸如数据中心常见的 A100、H100 等计算卡，由于本身没有物理 RT 核心（RT Cores），完全不支持该类高保真物理仿真环境的渲染工作，必须选用专门的工作站级或消费级 GPU（如 RTX 4080、Ada 6000 等）才能实现零拷贝的高速 GPU-Native 图像生成。

---

## 7. 结论与空地协同系统演进展望

基于 Gazebo 与 ROS 2 Humble 的「多旋翼 + 机械臂」空中操纵系统仿真，代表了目前传统物理控制与机器人操作生态的黄金结合方案。Gazebo 原生支持的 DART 物理引擎和 MoveIt 2 无缝耦合的关节规划接口，为验证重心偏移补偿控制、空中接触力控等高精尖算法提供了兼具效率和严谨性的数字化沙盒。

随着机器人技术的发展，未来的空中协同与操纵系统呈现出两大明显的技术演进趋势：

- **软硬件架构的端效应器中心化（End-Effector-Centric Paradigm）**：将多旋翼基座和关节臂割裂处理的传统分层控制逐渐被淘汰。前沿控制理论（如在 Flying Hand 平台中成功应用的架构）将整机动力学统一建模，向开发者提供唯一的、以端效应器坐标系为核心的交互层，使高层任务决策系统、模仿学习网络或人类遥操作输入无需感知多旋翼无人机的繁重姿态，从而降低了整个系统的决策负担。
- **走向多模态空地协同具身智能（Aero-Embodied Intelligence）**：空中操纵技术正与先进的大型视觉-语言-动作模型（VLA）加速融合。模型不再依靠简单的人工指定三维坐标来执行抓取，而是由大语言模型实时解析「去将灯塔上的坏白炽灯拧下来」这一自然语言指令，通过三维语义关键点检测器和视觉惯性里程计（VIO）来辅助多旋翼自主飞行并完成精细的三维接触力控操作。通过在数字孪生系统和高算力并行化仿真环境（如 Isaac Lab）中进行大规模动作轨迹生成与对抗强化训练，空中操纵系统的自主泛化作业能力将迎来飞跃性提升。

---

## 附：缺失项清单与跨文档一致性提示

### 缺失表格/图片（飞书未导出，需补图或表内容）

| 位置 | 缺失内容描述（据上下文推断） |
|------|------------------------------|
| 第 1 节 | Gazebo / Isaac Sim / AirSim(Colosseum) 三大仿真栈横向对比表 |
| 第 2 节 | ODE vs DART 物理引擎数值表现与适用性对比表 |
| 第 2 节 | 欠驱动基座 vs 全驱动基座对比表 |
| 第 3 节 | 开源核心组件 / 仿真框架 / 学术研究仓库地址与软件环境汇总表 |
| 第 4 节 | 常见 ROS/Gazebo 环境配置项表（配置项、作用、设置位置） |
| 第 6 节 | 三种计算负载（轻/中/高）标准化硬件推荐配置参数表 |

### 与第一份《仿真控制方案》文档的不一致点（落地时需注意对齐）

| 维度 | 第一份（仿真控制方案） | 本份（选型/建模报告） | 影响 |
|------|------------------------|------------------------|------|
| Gazebo 版本 | Gazebo Sim（新版，用 `gz_ros2_control`） | Ignition Fortress（用 `ign_ros2_control`） | 二者 API/包名不同，需选定其一 |
| 控制插件包 | `ros-humble-gz-ros2-control` | `ros-humble-ign-ros2-control` | Fortress 已老旧，建议统一到新版 Gazebo |
| Python 版本 | 3.10 | 3.11 | 统一到同一环境版本 |
| 机械臂/规划栈 | SO-100（`brukg/SO-100-arm` + `so_arm_100_hardware`）+ LeRobot | `medhijk/Aerial-Manipulator` + `SYSU-HILAB/am-planner` + Flying Hand | 两套不同机器人本体，需确认你实际用哪套 |
| 飞控策略 | PX4 `control_allocator` 全驱动分配 | 建议绕过 PX4，自写 ROS 2 几何控制/MPC/Whole-Body MPC | 路线分歧：用 PX4 全驱动 vs 自写 MPC |

### 文字修正

- 原文「物流自 Q 动化」修正为「物流自动化」。
- 原文「快速迭代调试调试」修正为「快速迭代调试」。
