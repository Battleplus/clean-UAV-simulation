# 仿真环境整理记录（2026-08-10）

## 唯一正式项目入口

- Windows：`E:\清洁无人机\drone_sim_ws`
- WSL：`/home/asus/my_drone_ws`
- `my_drone_ws` 是指向 Windows 正式工作区的软链接，不是第二份安装。

已移除重复入口 `/home/asus/clean_uav_ws` 和 Windows 根目录下的
`clean_uav_ws` 链接，避免从不同名称启动同一工作区。

## 必须保留的外部依赖

| 路径 | 用途 | 结论 |
|---|---|---|
| `/home/asus/PX4-Autopilot` | PX4 SITL 和自定义 Gazebo 桥接 | 保留 |
| `/home/asus/ros2_px4_build_ws` | `px4_msgs` 消息和 DDS 运行依赖 | 保留 |
| `/home/asus/drone_ws` | 原始机械臂 URDF/网格备份 | 保留 |
| `E:\清洁无人机\零件` | 正式 CAD 几何来源 | 保留 |
| `E:\清洁无人机\original_zip_model` | 原始模型归档 | 保留 |

PX4 的约 20 GB 占用主要来自 `PX4-Autopilot/build`，是当前 SITL
可执行文件和自定义桥接代码的构建结果，不是重复安装。PX4 源码目录
仍有本项目所需的本地修改，未将其误删。

## 已清理项目

- `/home/asus/drone_sim_ws_codex`：旧工作区；对比后无独有源码。
- Windows 与 WSL 中的 `PX4-main.tar.gz`：已解压仓库的重复压缩包。
- `_wsl_extract`：临时解压目录。
- pytest、Python、colcon 运行缓存和可重建日志。
- 重复的 `clean_uav_ws` 软链接。
- 未跟踪的参数试验 `.log/.status/.exit`；只保留验收证据与汇总文档。

## 正确的环境加载顺序

```bash
source /opt/ros/jazzy/setup.bash
source /home/asus/ros2_px4_build_ws/install/setup.bash
source /home/asus/my_drone_ws/install/setup.bash
```

第二行不能省略；`px4_ros2_control` 运行时需要其中的 `px4_msgs`。

## 单实例约束

人工 WASD 启动器会先终止遗留的 `dds_wasd_control`，再启动唯一实例。
两个 Offboard 控制节点同时发布会造成速度/位置目标冲突，表现为无法悬停、
WASD 断续或无响应。

## 不应提交云端的内容

- `build/`、`install/`、`log/`、`.pytest_cache/`、`__pycache__/`
- PX4 上游完整源码和其 20 GB 构建目录
- 参数迭代产生的临时运行日志
- 任何访问令牌或密钥

云端保存当前项目源码、配置、正式模型、补丁、测试、启动脚本、验收文档
和少量关键验收日志。
